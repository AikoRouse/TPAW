import tienda from "./catalogo.js"

const contenedor = document.getElementById("contendor")

contenedor.innerHTML="Hola Tienda"

let contendorHTML = ""

tienda.articulos.array.forEach(element => {
    
});